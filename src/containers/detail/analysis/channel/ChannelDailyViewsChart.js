import React from 'react';
import styled from 'styled-components';

const ChannelDailyViewsChartBlock = styled.div`
    width: 1080px;
    height: auto;
`;

const ChannelDailyViewsChart = () => {
    return (
        <ChannelDailyViewsChartBlock>
            채널 일일 조회수
        </ChannelDailyViewsChartBlock>
    );
};

export default ChannelDailyViewsChart;
