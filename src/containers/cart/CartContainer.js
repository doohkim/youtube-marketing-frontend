import React from 'react';
import styled from 'styled-components';

const CartContainerBlock = styled.div`
    width: 1080px;
    height: auto;
`;

const CartContainer = () => {
    return <CartContainerBlock>카트</CartContainerBlock>;
};

export default CartContainer;
